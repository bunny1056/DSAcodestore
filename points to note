BFS traversal
Queue can store positions

res = defaultdict(list)
This creates a dictionary where each key (a column index) maps to a list.
defaultdict from the collections module automatically creates an empty list if a key doesn’t exist when you try to access it.

def L_sym(arr):
            return arr == arr[::-1]

for maintaining visited list use set for O(1) lookup
